'use strict';

/**
 * Module dependencies
 */

var hbs = require('express-hbs');

function tags(options) {

  var output = '';

  return new hbs.handlebars.SafeString(output);
}

module.exports = tags;